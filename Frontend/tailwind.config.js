export const theme = {
    extend: {
      colors: {
        primary: "#2563EB", // Custom blue color
      },
      animation: {
        fadeIn: "fadeIn 0.5s ease-in-out",
      },
      keyframes: {
        fadeIn: {
          "0%": { opacity: "0" },
          "100%": { opacity: "1" },
        },
      },
    },
  };
  
  export const plugins = [];
  